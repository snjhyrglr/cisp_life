<p style="color: green"><%= notice %></p>

<%= render @rider_add_tpd %>

<div>
  <%= link_to "Edit this rider add tpd", edit_rider_add_tpd_path(@rider_add_tpd) %> |
  <%= link_to "Back to rider add tpds", rider_add_tpds_path %>

  <%= button_to "Destroy this rider add tpd", @rider_add_tpd, method: :delete %>
</div>
