json.extract! quote, :id, :product_id, :name, :description, :cooperative_id, :cooperative_branch_id, :gross_prem, :service_fee, :net_prem, :created_at, :updated_at
json.url quote_url(quote, format: :json)
